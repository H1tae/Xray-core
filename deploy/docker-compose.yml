name: xray

services:
  xray:
    image: ${XRAY_DOCKER_IMAGE:-eiravpn-xray:latest}
    restart: unless-stopped
    network_mode: host
    volumes:
      - ${XRAY_CONFIG_DIR:-.}:/app/configs:ro
    command: ["run", "-config", "/app/configs/server.json"]
